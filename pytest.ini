[pytest]
testpaths = tests
pythonpath = .
# An unregistered mark is a typo that silently disables a test, so make it an error.
addopts = --strict-markers
markers =
	slow: spawns a second interpreter or otherwise costs a noticeable share of the suite.
		A dev loop can skip these with -m "not slow"; CI must not.
